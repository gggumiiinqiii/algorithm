/**
 * @param {number} N
 * @return {number[][]}
 */
var specialGrid = function (N) {
  let gird = Math.pow(2, N);
  let num1 = Math.pow(2, 2 * N) - 1;
  console.log(gird, num1);
  let arr = [];
  function geneRateMatrix() {}
};
specialGrid(2);
