const minSubArrayLen = require("../../滑动窗口/209.长度最小的子数组");
minSubArrayLen(7, [2, 3, 1, 2, 4, 3]); //[4,3]
minSubArrayLen(4, [1, 4, 4]); //[4]
minSubArrayLen(5, [1, 2, 0, 3, 1, 1, 1]); //3[2,0,3]
minSubArrayLen(11, [1, 1, 1, 1, 1, 1, 1, 1]);
// 1.初始化左指针和右指针，分别指向数组的起始位置。
// 2.移动右指针，右指针到达数组末尾。
// 3.记录当前子数组的长度。
// 4.移动左指针，缩小子数组长度，直到子数组的和小于目标值。
// 重复步骤 2 到 4，直到右指针到达数组末尾。
// 在过程中记录最短的子数组长度。
